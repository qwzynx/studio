# Videos

Drop 16:9 video thumbnails here (e.g. `short.jpg`), then set the matching
`thumbnail` field in `app/components/videos.tsx` to `"/videos/short.jpg"`.

Set `link` to the YouTube/Vimeo URL to make the whole card clickable.
